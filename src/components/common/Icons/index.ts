import { default as IconClose } from './Close';
import { default as DownTriangle } from './DownTriangle';
import { default as UpTriangle } from './UpTriangle';
import { default as Search } from './Search';

export const Icons = {
  DownTriangle,
  UpTriangle,
  Close: IconClose,
  Search,
};

export default Icons;
